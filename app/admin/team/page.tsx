import { AdminPlaceholder } from "@/components/layout/admin-placeholder"

export default function AdminTeamPage() {
  return <AdminPlaceholder title="Team" description="Owner, admin, manager, order staff, and catalogue staff roles will be enforced through Supabase RLS and server checks." />
}
