import { AdminPlaceholder } from "@/components/layout/admin-placeholder"

export default function AdminCouponsPage() {
  return <AdminPlaceholder title="Coupons" description="Coupon validation will be server-side and tied to redemption limits, dates, and eligible products." />
}
