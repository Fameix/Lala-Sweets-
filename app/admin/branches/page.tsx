import { AdminPlaceholder } from "@/components/layout/admin-placeholder"

export default function AdminBranchesPage() {
  return <AdminPlaceholder title="Branches" description="Branch names, addresses, hours, pickup support, and delivery support must be verified here before public display." />
}
