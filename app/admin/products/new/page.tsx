import { AdminPlaceholder } from "@/components/layout/admin-placeholder"

export default function AdminNewProductPage() {
  return <AdminPlaceholder title="New Product" description="New products require reviewed name, category, image status, food type, price in paise, and branch assignment before ordering can be enabled." />
}
