import { SimplePage } from "@/components/layout/simple-page"

export default function ContactPage() {
  return (
    <SimplePage
      title="Contact"
      description="Contact numbers and WhatsApp details are treated as needs-review until the bakery confirms the official support channels."
    />
  )
}
