import { SimplePage } from "@/components/layout/simple-page"

export default function AboutPage() {
  return (
    <SimplePage
      title="About Master Bakery"
      description="Brand history and business details are awaiting owner verification, so this page avoids unverified claims for now."
    />
  )
}
