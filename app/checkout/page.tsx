import { SimplePage } from "@/components/layout/simple-page"

export default function CheckoutPage() {
  return (
    <SimplePage
      title="Checkout"
      description="Guest checkout, delivery slots, pickup, COD, and Razorpay will activate after server-side cart validation and payment verification are configured."
    />
  )
}
