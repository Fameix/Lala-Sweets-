import { SimplePage } from "@/components/layout/simple-page"

export default function OffersPage() {
  return (
    <SimplePage
      title="Offers"
      description="Offers and coupons are admin-managed. No public discount is shown until it is configured and active."
    />
  )
}
