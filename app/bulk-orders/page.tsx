import { SimplePage } from "@/components/layout/simple-page"

export default function BulkOrdersPage() {
  return (
    <SimplePage
      title="Bulk Orders"
      description="Bulk, event, and corporate order enquiries will route to the bakery team for quotation after the enquiry workflow is configured."
    />
  )
}
