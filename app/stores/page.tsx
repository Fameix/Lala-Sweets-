import { SimplePage } from "@/components/layout/simple-page"

export default function StoresPage() {
  return (
    <SimplePage
      title="Stores"
      description="Branch names, addresses, phone numbers, hours, pickup support, and delivery support must be verified in admin before public display."
    />
  )
}
