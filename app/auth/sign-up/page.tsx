import { SimplePage } from "@/components/layout/simple-page"

export default function SignUpPage() {
  return <SimplePage title="Create Account" description="Account creation is optional for checkout and can happen after ordering." />
}
