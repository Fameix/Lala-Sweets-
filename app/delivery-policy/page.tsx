import { SimplePage } from "@/components/layout/simple-page"

export default function DeliveryPolicyPage() {
  return (
    <SimplePage
      title="Delivery Policy"
      description="Delivery zones, fees, slot capacity, cutoff times, and pickup instructions will be controlled from admin settings."
    />
  )
}
