import { SimplePage } from "@/components/layout/simple-page"

export default function AccountFavouritesPage() {
  return <SimplePage title="Favourites" description="Favourites will become available after customer authentication and catalogue approval are configured." />
}
