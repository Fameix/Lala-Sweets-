import { SimplePage } from "@/components/layout/simple-page"

export default function AccountAddressesPage() {
  return <SimplePage title="Addresses" description="Saved addresses will be private to the customer and validated against configured delivery zones." />
}
