import { SimplePage } from "@/components/layout/simple-page"

export default function AccountOrdersPage() {
  return <SimplePage title="Orders" description="Order history will load only for the signed-in customer and will never expose another customer's orders." />
}
