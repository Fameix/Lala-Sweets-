import { SimplePage } from "@/components/layout/simple-page"

export default function AccountOrderDetailPage() {
  return <SimplePage title="Order Details" description="Order details will use public order numbers and verified customer access, not database IDs." />
}
