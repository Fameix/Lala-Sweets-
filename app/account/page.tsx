import { SimplePage } from "@/components/layout/simple-page"

export default function AccountPage() {
  return <SimplePage title="Account" description="Customer profile, addresses, favourites, order history, and custom cake requests will be available after Supabase Auth is connected." />
}
