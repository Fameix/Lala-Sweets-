# AI Admin Guide

Admin routes:

- `/admin/settings/cake-serving-rules`
- `/admin/ai-settings`
- `/admin/custom-cakes/[id]`

Secrets are controlled through environment variables and are not displayed after configuration. The current admin pages expose preview status until full Supabase persistence is connected.

