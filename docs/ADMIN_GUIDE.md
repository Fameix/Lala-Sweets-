# Admin Guide

Set `ADMIN_PREVIEW_ENABLED=true` locally to preview admin pages before Supabase role-based access is connected.

Catalogue review is the first admin workflow. Staff must fill prices, images, branch availability, food classification, variants, and stock before enabling ordering.
