# Decisions

- Keep all extracted products non-orderable until admin review is complete.
- Use `ADMIN_PREVIEW_ENABLED` as a temporary server-side admin gate before Supabase roles are connected.
- Do not publish unverified branch, phone, review, price, GST, FSSAI, or delivery claims.
- Use extracted JSON as the temporary catalogue source until Supabase is connected.
- Use Pexels images only as temporary placeholders and label them as non-client product photography.
