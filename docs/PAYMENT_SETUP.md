# Payment Setup

Razorpay Standard Checkout must be implemented with server-side cart validation, internal pending orders, Razorpay order creation, signature verification, webhook verification, idempotent status updates, and duplicate fulfilment protection.

Client-side success must never confirm an order by itself.
